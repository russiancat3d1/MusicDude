# Increase memory allocation for the Gradle daemon to prevent out-of-memory errors
org.gradle.jvmargs=-Xmx3g -XX:+HeapDumpOnOutOfMemoryError -Dfile.encoding=UTF-8

# Enable parallel and caching features to make the build much faster
org.gradle.parallel=true
org.gradle.caching=true
